# 子&然 小窝 (Love) 项目指南

## 会话启动与上下文交接
- **自动读取 HANDOFF.md**：在每次会话开始时，先读取项目根目录下的 `HANDOFF.md` 以加载最新的项目进度与架构全景，然后再关注并回答用户问题。若文件不存在则忽略。

## 架构与技术栈
- 微信小程序原生 + 微信云开发（CloudBase）。
- 所有涉及数据写入/积分变动的操作，必须通过云函数 `cloudfunctions/api` 完成，严禁在前端直接操作敏感数据库集合。
